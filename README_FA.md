# chagas-ecg-diagnosis

[🇬🇧 Click here for the English README](README.md)

تشخیص خودکار بیماری شاگاس از نوار قلب (ECG) 12-لید با استفاده از یادگیری گروهی (Ensemble Learning) و شبکه‌های عصبی عمیق (ResNet/Transformers). توسعه‌یافته برای مسابقه PhysioNet 2025.

## 🧠 رویکردها

### یادگیری ماشین کلاسیک (`team_code_classic_machine_learning.py`)
این رویکرد بر پایه مهندسی ویژگی‌های دستی و ترکیبی (Ensemble) از مدل‌های یادگیری ماشین سنتی استوار است:
- **استخراج ویژگی:** استخراج ویژگی‌های مورفولوژیکی (آماری، RMS، عبور از صفر)، حوزه فرکانس (PSD در باندهای مختلف) و HRV (تغییرپذیری ضربان قلب / فواصل R-R) در طول 12 لید نوار قلب.
- **متعادل‌سازی داده‌ها:** رفع عدم تعادل کلاس‌ها از طریق وزن‌دهی نمونه‌ها بر اساس منبع داده (کاهش وزن برچسب‌های ضعیف CODE-15%) و استفاده از روش نمونه‌برداری مجدد SMOTE + ENN.
- **دسته‌بند گروهی (Ensemble Classifier):** ترکیب رأی‌گیری نرم (soft-voting) شامل جنگل تصادفی (Random Forest) (40%)، گرادیان بوستینگ (Gradient Boosting) (40%) و رگرسیون لجستیک (Logistic Regression) (20%)، با استفاده از یک آستانه تصمیم‌گیری بهینه‌شده.

### شبکه عصبی کانولوشن یک‌بعدی (1D CNN) (`team_code_1D_CNN.py`)
این رویکرد از یک معماری سفارشی 1D ResNet استفاده می‌کند که مستقیماً روی سیگنال‌های خام نوار قلب به‌همراه داده‌های چندوجهی دموگرافیک آموزش دیده است:
- **پیش‌پردازش (Preprocessing):** مرتب‌سازی مجدد لیدها به ترتیب استاندارد 12-لید، نمونه‌برداری مجدد به فرکانس 500 هرتز، نرمال‌سازی با استفاده از نمره استاندارد (Z-score) برای هر لید و برش/پدینگ (padding/cropping) سیگنال به یک پنجره زمانی ثابت 10 ثانیه‌ای (5000 نمونه). همچنین سن و جنسیت از فایل‌های هدر نرمال‌سازی می‌شوند.
- **معماری:** یک نسخه یک‌بعدی اقتباس‌شده از ResNet-18. این مدل دارای یک میدان دریافت اولیه وسیع (`kernel_size=51`) برای ثبت ماکرو-شکل‌موج نوار قلب است. ویژگی‌های سن و جنسیت قبل از رسیدن به آخرین لایه کاملاً متصل (fully connected) الحاق (concatenate) می‌شوند. برای تنظیم‌مندی (regularization) از `Dropout(p=0.5)` استفاده شده است.
- **استراتژی آموزش:** با تابع زیان `BCEWithLogitsLoss` آموزش می‌بیند. ناترازی کلاس‌ها به‌صورت پویا محاسبه می‌شود و یک محدودیت `pos_weight` اعمال می‌گردد تا جریمه سنگینی برای از دست دادن کلاس اقلیت (مثبت) در نظر گرفته شود. از بهینه‌ساز AdamW (با نرخ یادگیری 1e-3) استفاده شده است.
- **موتور اعتبارسنجی:** توقف زودهنگام (Early Stopping) داخلی که در برابر یک پوشه اعتبارسنجی جداگانه (`validation_data` که توسط `split_data.py` ایجاد شده) ارزیابی می‌شود. شامل یک بهینه‌سازی آستانه تصمیم‌گیری پویا (grid-search) است که برای به حداکثر رساندن معیار F-measure بر روی احتمالات اعتبارسنجی طراحی شده است.

## 📊 نتایج ارزیابی (Benchmarks)

| مدل | امتیاز مسابقه (Challenge Score) | AUROC | AUPRC | دقت (Accuracy) | F-measure |
| :--- | :---: | :---: | :---: | :---: | :---: |
| یادگیری ماشین کلاسیک | 0.629 | 0.913 | 0.611 | 0.984 | 0.611 |
| شبکه 1D CNN | 0.442 | 0.856 | 0.316 | 0.966 | 0.327 |
